/**
 * @desc reducer
 */

// 默认 state
const defaultState = {
  inputVal: 'please input sth...',
  list: [1, 2, 3],
};

function reducer(prevState = defaultState, action) {
  return prevState;
}

export default reducer;
